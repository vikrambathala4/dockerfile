name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-nginx-app-git:$(date +%s)
           docker login --username="vikrambathala" --password="Homeboxoffice@1"
           docker tag my-nginx-app-git:$(date +%s) vikrambathala/my-nginx-app
           dokcer push my-nginx-app-git:$(date +%s)
